/**
 * Viewport-relative layout for the Engagement Trends modal.
 * Covers ~80% of the end-user screen on any device size.
 */
export const ENGAGEMENT_TRENDS_MODAL_SIZE = {
  width: '80vw',
  maxWidth: '80vw',
  height: '80vh',
  maxHeight: '80vh',
} as const;
