/**
 * Content Analytics modal canvas — matches Optimise Profile dialog dimensions.
 */
export const CONTENT_ANALYTICS_MODAL_SIZE = {
  width: "min(97vw, 1820px)",
  maxWidth: "min(97vw, 1820px)",
  maxHeight: "min(98dvh, calc(100dvh - 8px))",
} as const;

export const CONTENT_ANALYTICS_MODAL_CLASS = "linkedin-content-analytics-modal";
