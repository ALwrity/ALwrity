/** Viewport sizing for the unified Grow Network modal (Plan wedge–aligned wide canvas). */
export const GROW_NETWORK_MODAL_SIZE = {
  width: "min(97vw, 1200px)",
  maxWidth: "min(97vw, 1200px)",
  height: "min(98dvh, calc(100dvh - 8px))",
  maxHeight: "min(98dvh, calc(100dvh - 8px))",
} as const;
