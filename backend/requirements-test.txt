# Minimal dependencies for running pytest suites in CI.
# The full requirements.txt (~6GB with torch/tensorflow/nvidia/scrapy)
# is NOT needed — the test framework stubs all LLM/image modules.
#
# Add packages here only if a test module fails to import without them.

# Core frameworks
fastapi>=0.109.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
sqlalchemy>=2.0.0
starlette>=0.35.0

# Test runners
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-mock>=3.14.0

# HTTP / networking
httpx>=0.24.0
requests>=2.31.0
anyio>=4.0.0

# Auth (imported by middleware, stubbed by framework)
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.6

# Logging
loguru>=0.7.0

# Project-specific dependencies needed at import time
python-dotenv>=1.0.0
aiohttp>=3.9.0
aiofiles>=23.0.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# OAuth / token exchange dependencies
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.0
